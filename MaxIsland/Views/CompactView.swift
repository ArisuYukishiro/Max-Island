import SwiftUI

struct CompactView: View {
    var body: some View {
        HStack(spacing: 6) {
//            Circle()
//                .fill(Color.green)
//                .frame(width: 12, height: 12)
        }
        .transition(.scale.combined(with: .opacity))
    }
}
